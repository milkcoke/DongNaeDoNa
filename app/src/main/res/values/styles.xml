<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
    <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="powerSpinner">
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">20sp</item>
        <item name="android:background">@drawable/spinner_bottom_line_background</item>
        <item name="spinner_arrow_show">true</item>
        <item name="spinner_arrow_animate">true</item>
        <item name="spinner_arrow_gravity">end</item>
        <item name="spinner_arrow_tint">@color/primaryTextColorTransparent</item>
        <item name="spinner_arrow_padding">1dp</item>
<!--        app namespace is omitted in styles.xml-->
        <item name="spinner_divider_color">@color/colorTransparentWhite</item>
        <item name="spinner_divider_show">true</item>
        <item name="spinner_popup_animation">dropdown</item>
        <item name="spinner_popup_elevation">10dp</item>
        <item name="spinner_popup_background">@color/primaryTextColorTransparent</item>
<!--        <item name="spinner_divider_size">5dp</item>-->
    </style>


    <!--designs for login and register-->
    <style name="parent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="loginParent" parent="parent">
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/loginBkColor</item>
        <item name="android:focusableInTouchMode">true</item>
    </style>

    <style name="headerText" parent="parent">
        <item name="android:textSize">@dimen/headerTextSize</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textColor">@color/whiteTextColor</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="loginCard" parent="parent"></style>

    <style name="linearParent" parent="parent">
        <item name="android:orientation">vertical</item>
    </style>

    <style name="headerTextPrimary" parent="headerText">
        <item name="android:textColor">@color/primaryTextColor</item>
    </style>

    <style name="viewParent">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="loginButton" parent="viewParent">
        <item name="android:background">@drawable/login_shape_bk</item>
        <item name="android:textColor">@color/whiteCardColor</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>